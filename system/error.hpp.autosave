#ifndef __ERROR_HANDLER__
#define __ERROR_HANDLER__

#include "../kstd/data_types.hpp"

enum ErrorKernel
{
    ERROR_PMM_NOT_ENOUGH_MEMORY
};

namespace kernel
{

struct ErrorInfo
{
    uint error_code;
    uint error_importance;
    char *error_info;
};

class ErrorHandler
{
public:
    ErrorHandler();
    ~ErrorHandler();
    
    void InitErrorHandler();
    
    void Handle();
};

}
#endif // __ERROR_HANDLER__
